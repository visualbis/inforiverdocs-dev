---
id: text
title: TEXT
---
import useBaseUrl from "@docusaurus/useBaseUrl"; // Add to the top of the file below the front matter.
import Link from '@docusaurus/Link';

The `TEXT` function lets you change the way a number appears by applying formatting to it with format codes.

## Syntax

```javascript
TEXT(Value you want to format, "Format code you want to apply")
```

## Example

* To format the currency with a thousands separator and 2 decimals, like $6,000.84:

```javascript
TEXT(6000.839,"$#,###.00")
```

## Text Formatting Options

To explore further custom formatting options, checkout the editable formatting builder below.

You can select the formatting type from "Number, Large Number, Fraction, Percent, Currency, Date, or Time". Enter the desired string format in the **Format Code** box. The example values change accordingly.

<iframe width="100%" height="550" src="https://bryanbraun.gitlab.io/custom-formats/" />

Credit reference: [Custom Formats Builder](https://bryanbraun.gitlab.io/custom-formats/)