---
id: ifna
title: IFNA
---
import useBaseUrl from "@docusaurus/useBaseUrl"; // Add to the top of the file below the front matter.
import Link from '@docusaurus/Link';

The `IFNA` function returns the value you specify if a formula returns the #N/A error value; otherwise it returns the result of the formula.

## Syntax

```javascript
IFNA(value, value_if_na)
```

## Arguments

* **value**: The argument that is checked for the #N/A error value.
* **value_if_na**: The value to return if the formula evaluates to the #N/A error value.

## Example

Returns the result of the calculation `(AC-PY)/PY` if the value is a number else in case of an error it returns 0.

```javascript
IFNA((AC-PY)/PY, 0)
```
